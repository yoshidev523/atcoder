def calc(now_point, sushi, cost):
    right_index = (now + 1) % len(sushi)
    left_index = (now - 1) % len(sushi)
    

if __name__ == '__main__':
    n, c = map(int,raw_input().split())
    sushi = []
    x = []
    v = []
    for i in range(n):
        a, b = map(int,raw_input().split())
        c = {'point': a, 'coloie': b 'done': False}
        sushi.append(c)
    now_point = 0
    cost = 0

    print calc(now_point, sushi, cost)
    if abs(now_point - sushi)
    print -16 % 20
